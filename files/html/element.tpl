<div class="fund-thermo">
	<span class="goal">${{goal}}</span>
	<span class="glass">
		<span class="total" style="bottom: calc({{raised}} * 100% / {{goal}})">${{raised}}</span>
		<span class="amount" style="height: calc({{raised}} * 100% / {{goal}})"></span>
	</span>
	<div class="bulb">
		<span class="circle"></span>
		<span class="filler"></span>
	</div>
</div>